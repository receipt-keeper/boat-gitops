name: validate

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  helm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: azure/setup-helm@v4
        with:
          version: v3.18.4

      - name: Lint dev chart
        run: helm lint charts/boat-backend -f charts/boat-backend/values-dev.yaml

      - name: Lint prod chart
        run: helm lint charts/boat-backend -f charts/boat-backend/values-prod.yaml

      - name: Render dev chart
        run: helm template boat-backend charts/boat-backend -f charts/boat-backend/values-dev.yaml > /tmp/boat-dev.yaml

      - name: Render prod chart
        run: helm template boat-backend charts/boat-backend -f charts/boat-backend/values-prod.yaml > /tmp/boat-prod.yaml

      - name: Check rendered manifests
        run: |
          test -s /tmp/boat-dev.yaml
          test -s /tmp/boat-prod.yaml
          grep -q "kind: Deployment" /tmp/boat-dev.yaml
          grep -q "kind: StatefulSet" /tmp/boat-dev.yaml
          grep -q "kind: Job" /tmp/boat-dev.yaml
          grep -q "kind: Application" argocd/applications/boat-backend-dev.yaml
          grep -q "kind: Application" argocd/applications/boat-backend-prod.yaml

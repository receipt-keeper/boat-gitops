environment: dev

namespace:
  name: boatlab-dev

image:
  tag: sha-d77c6ff

backend:
  replicas: 1

postgresql:
  persistence:
    size: 5Gi

ingress:
  hosts:
    - host: boatlab-dev.luigi99.cloud
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: boatlab-dev-tls
      hosts:
        - boatlab-dev.luigi99.cloud
